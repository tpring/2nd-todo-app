import React from 'react';
import TodoContainer from './components/TodoContainer';

const App = () => {
    return (
        <main>
            <TodoContainer />
        </main>
    );
};

export default App;
